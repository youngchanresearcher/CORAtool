% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/implicants.R
\name{cora_coverage_score}
\alias{cora_coverage_score}
\alias{cora_inclusion_score}
\title{Sufficiency statistics of a prime implicant}
\usage{
cora_coverage_score(x, ...)

cora_inclusion_score(x, ...)
}
\arguments{
\item{x}{A prime implicant, as returned by \code{\link[=cora_prime_implicants]{cora_prime_implicants()}}.}

\item{...}{Unused.}
}
\value{
A single number, or \code{NaN} when the denominator is empty.
}
\description{
The coverage score is the share of the cases showing the outcome that the
prime implicant covers. The inclusion score is the share of the cases the
prime implicant covers that show the outcome.
}
\examples{
df <- data.frame(A = c(1, 0, 1, 0), B = c(1, 0, 0, 1),
                 C = c(0, 1, 1, 0), OUT = c(1, 1, 0, 1))
pis <- cora_prime_implicants(cora_context(df, "OUT"))
cora_coverage_score(pis[[1]])
cora_inclusion_score(pis[[1]])
}
