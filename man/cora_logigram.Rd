% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logigram.R
\name{cora_logigram}
\alias{cora_logigram}
\alias{cora_logigram.default}
\alias{cora_logigram.cora_system}
\alias{cora_logigram.cora_system_multi}
\alias{cora_logigram.cora_context}
\title{Draw a two-level logic diagram}
\usage{
cora_logigram(x, ...)

\method{cora_logigram}{default}(
  x,
  color_or = "lightblue",
  color_and = "lemonchiffon",
  notation = c("case", "prime"),
  title = NULL,
  subtitle = NULL,
  show_terms = FALSE,
  ...
)

\method{cora_logigram}{cora_system}(x, title = NULL, subtitle = NULL, ...)

\method{cora_logigram}{cora_system_multi}(x, title = NULL, subtitle = NULL, ...)

\method{cora_logigram}{cora_context}(x, ...)
}
\arguments{
\item{x}{A character vector of functions in disjunctive normal form, such
as \code{"A*B+c*A+b<=>F"} or \code{"A{1}*B{2}+C{0}<=>F"}, one entry per
outcome.
Square brackets are read as curly ones, so the \code{"A[1]*B[2]"} notation of
the QCA package is accepted too.
A \code{\link[=cora_context]{cora_context()}} or a solution from \code{\link[=cora_irredundant_sums]{cora_irredundant_sums()}} or
\code{\link[=cora_irredundant_systems]{cora_irredundant_systems()}} is accepted directly and converted first.}

\item{...}{Passed to methods.}

\item{color_or}{Fill colour of the OR gates.}

\item{color_and}{Fill colour of the AND gates.}

\item{notation}{\code{"case"} when a negated literal is written in lower case,
\code{"prime"} when it is written with a trailing apostrophe.}

\item{title}{Text printed above the diagram, one line per element. The
default, \code{NULL}, prints the expression the diagram stands for; for a
solution object that is the solution itself, essential prime implicants
still marked with \verb{#}. Pass \code{NA} to print no title.}

\item{subtitle}{A single line printed under the title. The default,
\code{NULL}, prints the coverage and inclusion scores when the object drawn
carries them, and nothing otherwise. Pass \code{NA} to print no subtitle.}

\item{show_terms}{Print each conjunction next to the gate that forms it.
Useful when the diagram is read on its own, away from the solution.}
}
\value{
The parsed diagram, invisibly. Called for the plot it draws.
}
\description{
Renders a Boolean or multi-value function in disjunctive normal form as a
two-level logic diagram: conjunctions become AND gates, the disjunction
over them an OR gate. This is an R implementation of LOGIGRAM.
}
\examples{
cora_logigram("A*B+c*A+b<=>F")
cora_logigram("A{1}*B{2}+C{0}<=>F")

df <- data.frame(A = c(1, 0, 1, 0), B = c(1, 0, 0, 1),
                 C = c(0, 1, 1, 0), OUT = c(1, 1, 0, 1))
sol <- cora_irredundant_sums(cora_context(df, "OUT"))[[1]]

## The solution and its scores are written above the diagram.
cora_logigram(sol)

## Each conjunction next to its own gate, and no header.
cora_logigram(sol, title = NA, subtitle = NA, show_terms = TRUE)
}
